# ruff.toml

# Set line length limit
line-length = 120

[lint]
# Ignore common harmless warnings to keep CI lint check passing
ignore = [
    "F401",  # Unused imports
    "F841",  # Unused local variables
    "F541",  # f-string without placeholders
    "E402",  # Module level import not at top of file
]
